query ChannelSettings($channelId: Int!, $userId: Int) {
  channel(id: $channelId) {
    name
    id
    private
    passwordProtected
    owner {
      id
      name
      avatar
      rank
    }
    admins {
      id
      name
      avatar
      rank
    }
    members {
      id
      name
      avatar
      rank
    }
    banned {
      id
      endAt
    }
    muted {
      endAt
      id
    }
  }
  user(id: $userId) {
    id
  }
}
