query getInfoUsers($userId: Int) {
  user(id: $userId) {
    typename
    name
    avatar
    rank
    channels {
      typename
      name
      id
      messages {
        content
        sentAt
      }
    }
    friends {
      typename
      name
      messages {
        content
        sentAt
      }
      id
    }
  }
}
