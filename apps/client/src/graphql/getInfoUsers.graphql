query GetInfoUsers($userId: Int) {
  user(id: $userId) {
    id
    __typename
    name
    blocked
    blocking
    avatar
    rank
    channels {
      __typename
      name
      id
      messages {
        id
        content
        sentAt
      }
    }
    friends {
      __typename
      name
      avatar
      messages {
        content
        sentAt
      }
      id
    }
  }
}
