query InfoDirectMessages($userId: Int) {
  user(id: $userId) {
    name
    avatar
    rank
    messages {
      recipient {
        id
        name
        avatar
      }
      author {
        id
        name
        avatar
      }
      content
      sentAt
      readAt
    }
    blocked
    blocking
  }
}
