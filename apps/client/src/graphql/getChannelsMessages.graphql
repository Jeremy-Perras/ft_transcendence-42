query getChannelsMessages($name: String) {
  channels(name: $name) {
    messages {
      sentAt
      content
      readBy {
        user {
          name
        }
      }
      author {
        name
      }
    }
  }
}