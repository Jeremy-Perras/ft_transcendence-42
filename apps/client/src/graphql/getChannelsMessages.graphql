query getChannel($channelId: Int!) {
  channel(id: $channelId) {
    private
    passwordProtected
    name
    owner {
      name
      id
    }
    messages {
      id
      author {
        name
      }
      readBy {
        user {
          name
          avatar
        }
      }
      content
      sentAt
    }
    admins {
      name
      id
    }
    members {
      name
      id
    }
  }
}
