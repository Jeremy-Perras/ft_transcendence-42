query ChannelSettings($channelId: Int!) {
  channel(id: $channelId) {
    id
    name
    owner {
      id
      name
      avatar
    }
    admins {
      id
    }
    members {
      id
      name
      avatar
    }
    banned {
      id
      name
      avatar
      endAt
    }
    muted {
      id
      endAt
    }
    passwordProtected
    private
  }
}
