query ChannelDiscussion($channelId: Int!) {
  channel(id: $channelId) {
    banned {
      user {
        id
      }
      endAt
    }
    muted {
      endAt
      user {
        id
      }
    }
    private
    passwordProtected
    name
    owner {
      id
      name
      avatar
    }
    members {
      id
    }
    admins {
      id
    }
    messages {
      readBy {
        id
        name
        avatar
        status
      }
      content
      id
      sentAt
      author {
        id
        name
        avatar
        status
      }
    }
  }
}
