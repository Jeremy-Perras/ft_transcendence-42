query DiscussionsAndInvitations($userId: Int) {
  user(id: $userId) {
    id
    chats {
      avatar
      hasUnreadMessages
      id
      lastMessageContent
      lastMessageDate
      name
      type
      status
    }
    pendingFriends {
      id
      avatar
      name
    }
  }
}
