query DiscussionsAndInvitations($userId: Int) {
  user(id: $userId) {
    id
    chats {
      avatar
      hasUnreadMessages
      id
      lastMessageContent
      lastMessageDate
      name
      type
    }
    pendingFriends {
      id
      avatar
      name
    }
  }
}