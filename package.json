{
  "name": "ft_transcendence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev -w apps/client",
    "dev:server": "npm run dev -w apps/server",
    "dev:prisma": "npm run prisma:watch -w apps/server",
    "build:client": "npm run build -w apps/client",
    "build:server": "npm run build -w apps/server",
    "build:all": "npm run build:client && npm run build:server",
    "db:seed": "npm run prisma:seed -w apps/server",
    "db:deploy": "npm run prisma:push -w apps/server && npm run prisma:generate -w apps/server",
    "compile:shared": "npm run compile -w shared/schemas && npm run compile -w shared/game",
    "deploy": "npm run compile:shared && npm run db:deploy && npm run db:seed && npm run build:all && node apps/server/dist/main"
  },
  "workspaces": [
    "./apps/*",
    "./shared/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.40.0",
    "@typescript-eslint/parser": "^5.40.0",
    "eslint": "^8.25.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.7.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "zod": "^3.19.1"
  }
}
